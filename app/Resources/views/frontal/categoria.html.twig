{#Plantilla de nuestra aplicacion#}
{% extends 'base.html.twig' %}
{% block title %} Nuestro Equipo {% endblock %}
{# CONTENIDO #}
{% block contenido %}
            <section class="py-5">
                <div class="container px-5 my-5">
                    <div class="row gx-5 justify-content-center">
                        <div class="col-lg-6">
                            <div class="text-center mb-5">
                                <h1 class="fw-bolder">{{categoria.nombre}}</h1>
                                <p class="lead fw-normal text-muted mb-0">{{categoria.descripcion}}</p>
                            </div>
                        </div>
                    </div>
                    <div class="row gx-5">
                    
                        <div class="col-6"><img class="img-fluid rounded-3 mb-5" src="{{categoria.foto}}" alt="..." /></div>
                 
                    </div>
                    <div class="row gx-5 justify-content-center">
                        <div class="col-lg-6">
                            <div class="text-center mb-5">

                            </div>
                        </div>
                    </div>
                </div>
            </section>


{% endblock %}